# Rust build output — regenerated by cargo on every build, never commit it.
# (This is the workspace-wide `target/`, analogous to Gradle's `build/`.)
/target

# We deliberately DO commit Cargo.lock (recommended even for libraries now):
# it pins exact dependency versions for reproducible builds across machines.
